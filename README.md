# 背景

这是《人工智能》课程期末project

project方案：`方案（第2版）.md`

分工：

- A：邱名浩
- B：麻轩诚
- C：张易聆
- D：武旻阳



# 如何使用

- 把本仓库`git clone`到本地，然后让AI根据 `方案（第2版）.md `和 `README.md` 完成你的模块的任务即可
- 用好agent工具，最好直接让claude code或codex完成代码&报告，然后自己结合网页端AI简单做一下PPT。每个人整个工作流的用时应当可以控制在1-2小时内。
- 如果遇到困难首先问AI，不要在这个project上花费太多时间。不要尝试古法编程或手写报告，太耗时





# DDL

> [!NOTE]
>
> 由于A、B、C、D四个模块的代码存在上下游关系，**单独某个模块的代码无法做实验得到结果**，因此每个人在6月13日前交的内容里**不需做完整实验，只要做模块自测**，确保完整实现`方案（第2版）.md`中对该模块功能的要求即可。
>
> 代码分工更新：
>
> | 成员                           | 代码职责                                                     |
> | ------------------------------ | ------------------------------------------------------------ |
> | A 同学：Agent 调度与仿真主流程 | 实现 Agent 类、角色配置、时间步循环、Agent 间消息传递；~~只负责把各模块按约定接口串起来，不单独承担所有集成问题~~，==会做最终的代码整合，得到实验结果== |
> | B 同学：记忆、反思与关系演化   | 实现长期记忆、历史检索、观点状态更新、reflection prompt；同时负责社交关系权重更新规则。==依托A模块产物进行B模块自测即可== |
> | C 同学：辩论、投票与实验评估   | 实现 debate mode、confidence、vote；~~负责 2-3 组对比实验脚本、指标统计和结果表格~~。==依托A模块产物进行C模块自测即可== |
> | D 同学：前端可视化与现场 Demo  | 实现 Streamlit 页面、NetworkX/pyvis 关系图、传播曲线、演示脚本；负责把 A/B/C 导出的状态显示出来。==依托A模块产物进行D模块自测即可== |
>
> 【邱名浩】会在6月16日前完成代码的整合，做完整的实验，把得到的实验结果放到最终报告&最终PPT里



6月13日（周六）23:59前，【所有人】根据`方案（第2版）.md`，完成：

- 自己负责的那部分代码。放在`scripts_{分工编号}/`目录下。

- 运行代码得到的产物。放在`output_{分工编号}/`目录下。

- 自己负责的报告段落草稿。保存为`report_{分工编号}.md`。内容包括：

  - 粘贴该模块所有代码
  - 讲解该模块代码功能
  - **模块自测结果**
    - 因为单独的模块无法做完整实验，所以不需要实验结果
    - 可以先使用output_A数据进行自测，把结果放进报告

- 自己负责的PPT 草稿。保存为`{分工编号}.pptx`。要求：

  - 使用 `PPT模板.pptx`
  - 讲解该模块实现的功能
  - 分析**模块自测结果**
    - 因为单独的模块无法做完整实验，所以不需要实验结果
    - 可以先使用output_A数据进行自测，把结果放进PPT
  - 体现project的创新点
  - 页数不限，但是注意控制时间。展示总时长是8-10分钟，因此每个人只需讲2分钟左右，不要超过2.5分钟

- 将以上内容一起`git push`到本仓库。例如，A同学git push的内容应当包括：

  ```bash
  人工智能PROJECT
  │  A.pptx
  │  report_A.md
  │  
  ├─output_A
  │      api_usage_A.json
  │      state_A.json
  │      state_A_timestep_0.json
  │      state_A_timestep_1.json
  │      state_A_timestep_2.json
  │      state_A_timestep_3.json
  │      state_A_timestep_4.json
  │      
  └─scripts_A
      │  adapters.py
      │  agent.py
      │  llm_client.py
      │  main.py
      └─ __init__.py 
  ```
  
  
  

6月16日（周二）23:59前，【邱名浩】完成：

- 把4个人的代码整合，修改，确保互相兼容。做完整的实验，得到实验结果
- 把4个人的报告拼接，添加完整实验结果
- 把4个人的PPT拼接，添加完整实验结果



6月18日（周四）人工智能课上，【所有人】：

- 上台展示。（注意控制时间。展示总时长是8-10分钟，因此每个人只需讲2分钟左右，不要超过2.5分钟）







# 补充信息

 A 模块运行命令：

```text
A 提供的 mock/fallback 数据可用以下命令重新生成：
python scripts_A\main.py


# 不过，邱名浩的API没包含在仓库里。如果你想重新运行模块A，需要新建文件`.\qmh_API.md`，把你使用的API填进去
```



明确 B/C 的统一入口函数名：

```text
B 模块必须提供：
update_social_state(state, timestep) -> state

C 模块必须提供：
update_debate_and_metrics(state, timestep) -> state
```



明确 D 的数据源：

```text
D 前端先读取 output_A/state_A.json 开发和自测；最终整合后再替换为最终 state.json。
```

